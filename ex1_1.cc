/*
* Review the documentation for your compiler and determine what file naming
* convention it uses. Compile and run the "main" program from page 2.
*/

/*
* I am using the "clang" compiler on Mac OS X, which uses the Unix naming
* convention. Unless otherwise noted, all programs in this repo are compiled
* with the command "clang++ -std=c++11 -Weverything -o <executable> <source.cc>"
*/
int main()
{
	return 0;
}
