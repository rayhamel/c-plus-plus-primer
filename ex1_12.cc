/*
* What does the following "for" loop do? What is the final value of "sum"?
*
* int sum = 0;
* for (int i = -100; i <= 100; ++1)
*     sum += i;
*/

// It adds the integers in the range -100 to 100. The final value of "sum" is 0.
