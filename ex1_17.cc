/*
* What happens in the program presented in this section if the input values are
* all equal? What if there are no duplicated values?
*/

/*
* If the input values are all equal, the program will write
* "<n> appears <x> times" to the console only once. If there are no duplicated
* values, then "<n> appears 1 times" would be printed for every value.
*/
